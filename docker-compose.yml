version: '3.9'

services:
  redis:
    image: redis:latest

    container_name: tailr-redis

    restart: unless-stopped

    ports:
      - '${REDIS_PORT}:6379'

    volumes:
      - redis-data:/data

volumes:
  redis-data:
